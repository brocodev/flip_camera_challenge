name: flip_camera_challenge
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  cached_network_image: ^3.2.3
  camera: ^0.10.5+4
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter
  ui_common:
    git: https://github.com/brocodev/ui_common.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true
